# Copy to .env.local and adjust if needed.
# UI calls /api/* — Next.js proxies to the FastAPI backend (see next.config.js).
BACKEND_URL=http://127.0.0.1:8100

# Optional: bypass proxy and call backend directly (must match CORS_ORIGINS on API)
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8100
